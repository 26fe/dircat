# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/dircat.rb'

Hoe.new('dircat', Dircat::VERSION) do |p|
  # p.rubyforge_name = 'ralbum_commmonx' # if different than lowercase project name
  p.developer('Giovanni', 'tokiro.oyama@gmail.com')
end

# vim: syntax=Ruby

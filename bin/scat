# -*- coding: utf-8 -*-
#!/usr/bin/env ruby -w
cwd = File.expand_path(File.join(File.dirname(__FILE__), %w{.. lib}))
$:.unshift(cwd) unless $:.include?(cwd)

require 'simple_cataloger/core'
require 'simple_cataloger/cli'
CliCat.run!
